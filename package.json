{
  "dependencies": {
    "@gatsbyjs/parcel-namer-relative-to-cwd": "^2.5.0",
    "parcel": "^2.8.0",
    "parcel-reporter-static-files-copy": "^1.4.0",
    "rimraf": "^4.0.6"
  },
  "scripts": {
    "build": "rimraf dist && cd public && parcel build index.html"
  }
}